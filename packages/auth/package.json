{
  "name": "@ebringgs/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Shared auth + theme state and the ProtectedRoute guard — same login/role/theme behaviour across web, teacher, admin",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@ebringgs/api": "workspace:*",
    "@ebringgs/types": "workspace:*"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-router-dom": "^7.0.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "typescript": "~5.9.3"
  }
}
